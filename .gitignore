.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
outputs/
.DS_Store

# Model weights are pulled locally; only the manifest and the small upstream config/text files are committable
weights/**/*.safetensors
weights/**/*.pt
weights/**/*.pth
weights/**/*.ckpt
weights/**/*.bin
weights/**/.cache/

# the pinned dataset tarball and its extracted chips are fetched at run time
weights/multi-temporal-crop/

weights/**/*.tif
